#!/bin/sh
#|-*- mode:lisp -*-|#
#| create a user comments table
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  #+quicklisp (ql:quickload '(:kalesville) :silent t))

(defpackage :ros.script.001-user-comments-table.3684947233
  (:use :cl
        :sxql
	:kalesville.migrations))
(in-package :ros.script.001-user-comments-table.3684947233)

(defvar migration-id 001)

(defvar sql
  (create-table :user_comments
      ((id :type '(char 36) ;; length of uuid4
	   :primary-key t)
       (author :type '(varchar 60)
	       :not-null t)
       (text :type 'text
	     :not-null t))))


(defun main ()
  (km-create-table migration-id sql))
