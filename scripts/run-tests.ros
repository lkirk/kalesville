#!/bin/sh
#|-*- mode:lisp -*-|#
#| run the tests!
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  #+quicklisp (ql:quickload '(:kalesville-test) :silent t))

(defpackage :ros.script.run-tests.3687576339
  (:use :cl))
(in-package :ros.script.run-tests.3687576339)

(defun main ()
  (prove:run :kalesville-test))
